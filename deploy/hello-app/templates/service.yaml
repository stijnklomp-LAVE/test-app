apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-app.fullname" . }}
  labels:
    app: hello-app
spec:
  selector:
    app: hello-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http